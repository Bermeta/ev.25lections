GIT - распределенная  система контроля версий. Это система для отслеживания и ведения истории изменения файлов, в вашем проекте. 

Репозиторий - хранилище вашего кода и истории его изменений.

GitHub - Веб сайт для размещения git-репозиториев в совместной разработке проектов. 

Команды GIT:
1. git init - она создает новый локальный гит репозиторий у вас на ПК, в той директории в которой была прописана команда. (команда вводится один раз в начале)

2. git add - команда для добавления файлов в систему отслеживания гита, после этой команды изменения начинают отслеживаться гитом. 

3. git commit -m '<commet>' - как только достигаем определенного момента в разработке, то тогда мы сохраняем и комментурием изменения в локальный репозиторий. 

4. git remote add - команда для того чтобы связать наш локальный репозиторий с удаленным репозиторием, то есть добавляет подключение к репозиторий на гитхабе. 
	git remote add <name: origin> <ссылка_на_репо:ssh>

5. git push - команда для отправки всех изменений на локальном репозитории в удаленные репозиторий. 
	git push origin <main>


-------------------------------

1.git init (только один раз)
2.git add .
3.git commit -m ''
	3.1 git branch -M main (1 раз, только переименования ветки) 
 	*git remote add origin <ссылка> (только 1 раз)
4. git push origin main 

